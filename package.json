{
  "name": "cardapiogram-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp index.html public/ && cp menu.html public/ && cp index.css public/"
  },
  "dependencies": {
    "@google/genai": "^0.12.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}